* All Match/ Any Match

    boolean allMatch(Predicate<? super T> predicate);
    boolean anyMatch(Predicate<? super T> predicate);

    - allMatch - Stream 안의 모든 데이터가 predicate 를 만족하면 true
    - anyMatch - Stream 안의 데이터 중 하나라도 predicate 를 만족하면 true
