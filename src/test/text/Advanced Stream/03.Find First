* Find First
    Optional<T> findFirst();
    Optional<T> findAny();

    - findFirst - Stream 안의 첫번재 데이터를 반환. Stream 이 비어있다면
      비어있는 Optional 을 반환.
    - findAny - Stream 안의 아무 데이터나 리턴. 순서가 중요하지 않고 Parallel Stream 을 사용할 때 최적화를 할 수 있다.
      마찬가지로 Stream 이 비어있다면 빈 Optional 을 반환.